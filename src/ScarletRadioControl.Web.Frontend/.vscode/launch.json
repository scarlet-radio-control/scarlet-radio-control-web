{
	"$schema": "vscode://schemas/launch",
	"version": "0.2.0",
	"configurations": [
		{
			"name": "localhost (Edge)",
			"request": "launch",
			"type": "msedge",
			"url": "https://localhost:5173",
			"webRoot": "${workspaceFolder}"
		}
	]
}
